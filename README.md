# PA06-Template-1 Assignment Questions (worth 2.5 points each)

1. Why won't template function countOccurrences() handle all C++ types correctly?

2. Give three examples of types this function won't handle correctly.

3. Why does the starter code use the vector .at() method to fill vector vData? In other words, why not use .push_back()?

4. The starter code uses a nested for loop to count occurrences of each value in vData. Does the C++ STL provide a better way to do this? If so, what is that better way?